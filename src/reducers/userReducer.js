

const INITIAL_STATE = { loginSucess: false, loginData: null, userType:''} 

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {

    
        default:
            return state;
    }
}